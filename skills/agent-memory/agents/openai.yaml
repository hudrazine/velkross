interface:
  display_name: "Agent Memory"
  short_description: "Save, recall, and organize repo-local project context"
  default_prompt: "Use $agent-memory to save, recall, or organize durable project context for this repository."
